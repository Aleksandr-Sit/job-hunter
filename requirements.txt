# Версии закреплены 17.09.2026 по факту рабочего контейнера (pip freeze).
# Зачем жёстко: было `>=`, и любая пересборка молча тянула свежие версии —
# выход APScheduler 4 (другой API) уронил бы запуск, а смена openai SDK меняет
# поведение обработки ошибок провайдера. Обновлять осознанно: поднять версию,
# прогнать тесты на VPS, посмотреть стартовый прогон.

# AI-матчинг (OpenAI-совместимые провайдеры: OpenRouter, Groq, Cerebras)
openai==3.14.1

# HTTP и разбор HTML
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.3

# Telegram-бот (карточки и кнопки)
python-telegram-bot==22.8

# Планировщик
APScheduler==3.11.3

# Конфигурация
PyYAML==6.0.3
python-dotenv==1.2.3

# python-jobspy УДАЛЁН 17.09.2026: нигде не импортировался, но тянул в образ
# pandas и numpy (~300 МБ) на диске, где свободно было 1.3 ГБ.
