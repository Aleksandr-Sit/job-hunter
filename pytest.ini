[pytest]
testpaths = tests
python_files = test_*.py
# Корень проекта в sys.path: иначе `pytest` (без `python -m`) не находит src/ —
# именно на этом падал CI, хотя локальный `python -m pytest` проходил.
pythonpath = .
addopts = -q
